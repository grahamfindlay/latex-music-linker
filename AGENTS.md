Use `uv` whenever possible. For example:

- `uv run latex-music-linker ...` instead of `latex_music_linker ...`.
- `uv run pytest ...` instead of `pytest ...`.
- `uv run llm ...` instead of `llm ...`.
